<h2>
	Здесь вы можете занести свои контакты:
</h2>

<form action="/contacts" method="POST" role="form">

  <div class="form-group">
    <label>Ваше имя</label>
    <input name="username" type="text" class="form-control" placeholder="Ваше имя" value="<%= @username %>">
  </div>

  <div class="form-group">
    <label>Ваш email:</label>
    <input name="email" type="text" class="form-control" placeholder="Ваш email" value="<%= @email %>">
  </div>

    <button type="submit" class="btn btn-primary">Записаться</button>


  </form>

  <h3>
  	Перечень контактов:
  </h3>
  <br/>
  <% @contacts.each do |row| %>
  <%=row.name %>: <%= row.email %>
  <hr/>
  <% end %>